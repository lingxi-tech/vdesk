# Web project

This folder contains a minimal backend (FastAPI) and frontend (Vue 3 + Vite) to manage containers as described in the top-level README.

- backend: web/backend
- frontend: web/frontend

Follow the README files under each folder to run them.
